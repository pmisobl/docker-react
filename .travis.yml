sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t pmisobl/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true pmisobl/docker-react npm run test -- --coverage